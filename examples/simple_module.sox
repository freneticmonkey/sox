// simple_module.sox - Minimal module for basic testing

var message = "Hello from simple_module!"
var value = 42

var greet = fn(name) {
    print("Hello, " + name + "!")
}

var add = fn(a, b) {
    return a + b
}

// Export
return Table{
    "message": message,
    "value": value,
    "greet": greet,
    "add": add
}
